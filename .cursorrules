# Notes App — Project Rules

## Architecture

### Data Model

```
interface NoteSummary {
  id: string;
  slug: string;
  title: string;
  createdAt: string;
  updatedAt: string;
  attachmentCount: number;
  tags: string[];       // hierarchical, slash-separated (e.g. "dev/python/fastapi")
  pinned: boolean;
}

interface Note extends NoteSummary {
  content: string;
  attachments: Attachment[];
}

interface Attachment {
  id: string;
  originalName: string;
  mimeType: string;
  size: number;
  relativePath: string;
}

type PreviewMode = 'edit' | 'preview';

// TodoQuadrant derived from QUADRANT const in constants.ts, re-exported from types.ts
type TodoQuadrant = 'do' | 'schedule' | 'delegate' | 'planned';

interface QuadrantMeta { key: TodoQuadrant; label: string; description: string; }

interface Todo {
  id: string;
  title: string;
  description?: string;
  dueDate?: string;
  linkedNoteIds?: string[];
  quadrant: TodoQuadrant;
  completed: boolean;
  createdAt: string;
  updatedAt: string;
}
```

### Filesystem Layout

- Notes stored at `NOTES_ROOT/notes/YYYY-MM-DD-slug-uuid/`
- Each note folder contains `note.md` (frontmatter: id, title, tags, pinned, createdAt, updatedAt) + `attachments/`
- Slug format: `${date}-${slugifiedTitle}-${uuid}`
- Todos stored at `NOTES_ROOT/todos.json` (single JSON array)
- `NOTES_ROOT` configured via env var

### Constants (`lib/constants.ts`)

```
const PREVIEW_MODES: readonly PreviewMode[];
const PREVIEW_EDIT: PreviewMode;
const PREVIEW_PREVIEW: PreviewMode;

const QUADRANT: { DO: 'do'; SCHEDULE: 'schedule'; DELEGATE: 'delegate'; PLANNED: 'planned' };
type TodoQuadrant = (typeof QUADRANT)[keyof typeof QUADRANT];  // single source of truth
const QUADRANT_KEYS: readonly TodoQuadrant[];
const QUADRANT_META: readonly QuadrantMeta[];
```

### Core Functions (`lib/fsNotes.ts`)

```
listNotes(): Promise<NoteSummary[]>
getNote(id: string): Promise<Note | null>
createNote(input: {title: string, content: string, tags?: string[]}): Promise<Note>
updateNote(id: string, input: {title?, content?, tags?, pinned?}): Promise<Note>
deleteNote(id: string): Promise<void>
listAllTags(): Promise<string[]>
listAttachments(noteId: string): Promise<Attachment[]>
saveAttachment(noteId: string, file: File): Promise<Attachment>
deleteAttachment(noteId: string, attId: string): Promise<void>
```

### Tag Tree Utilities (`lib/tagTree.ts`)

```
interface TagNode { segment: string; fullPath: string; noteCount: number; children: TagNode[] }
buildTagTree(notes: NoteSummary[]): TagNode[]
getChildNodes(tree: TagNode[], path: string): TagNode[]
getNotesAtPath(notes: NoteSummary[], path: string): NoteSummary[]
getNotesUnderPath(notes: NoteSummary[], path: string): NoteSummary[]
```

### Core Functions (`lib/fsTodos.ts`)

```
listTodos(): Promise<Todo[]>
getTodo(id: string): Promise<Todo | null>
createTodo(input: {title, quadrant, description?, dueDate?, linkedNoteIds?}): Promise<Todo>
updateTodo(id: string, input: Partial<...>): Promise<Todo>
deleteTodo(id: string): Promise<void>
```

### API Routes

```
app/api/notes/route.ts              → GET, POST
app/api/notes/[id]/route.ts         → GET, PUT, DELETE
app/api/notes/[id]/attachments/route.ts        → GET, POST
app/api/notes/[id]/attachments/[attId]/route.ts → DELETE
app/api/notes/[id]/attachments/[attId]/download/route.ts → GET
app/api/todos/route.ts              → GET, POST
app/api/todos/[id]/route.ts         → GET, PUT, DELETE
```

- NextRequest/NextResponse
- Zod validation on every endpoint
- Error shape: `{ error: string }`
- POST/PUT notes accept `tags: string[]` and `pinned: boolean`
- POST/PUT todos accept `linkedNoteIds: string[]`

### Components

| Component | Location | Notes |
|---|---|---|
| MarkdownEditor | `components/MarkdownEditor.tsx` | `"use client"`, `[[` trigger + Cmd+L for note linking, internal link renderer |
| NoteLinkPicker | `components/NoteLinkPicker.tsx` | `"use client"`, reusable note search dialog (CommandDialog) |
| AttachmentList | `components/AttachmentList.tsx` | Card, Badge, Button |
| FileUpload | `components/FileUpload.tsx` | Drag & drop, Button, Card |
| ThemeToggle | `components/ThemeToggle.tsx` | `"use client"`, Button + lucide icons |
| AppSidebar | `app/(app)/AppSidebar.tsx` | `"use client"`, tabs for Notizen/Aufgaben |
| NotesSidebarContent | `app/(app)/NotesSidebarContent.tsx` | `"use client"`, pinned + Tags/Alle toggle |
| TagBrowser | `app/(app)/TagBrowser.tsx` | `"use client"`, folder-style drill-down tag navigation |
| TodosSidebarContent | `app/(app)/TodosSidebarContent.tsx` | `"use client"`, quadrant summary |
| EisenhowerMatrix | `app/(app)/todos/EisenhowerMatrix.tsx` | `"use client"`, 2x2 grid |
| QuadrantCard | `app/(app)/todos/QuadrantCard.tsx` | `"use client"`, single quadrant |
| TodoCard | `app/(app)/todos/TodoCard.tsx` | `"use client"`, Checkbox + title + linked note badges |
| TodoDialog | `app/(app)/todos/TodoDialog.tsx` | `"use client"`, create/edit Dialog + note linking |
| MobileBottomNav | `app/(app)/MobileBottomNav.tsx` | `"use client"`, bottom tab bar on mobile (`md:hidden`), uses `.card-base` |
| MobileSidebarFab | `app/(app)/MobileSidebarFab.tsx` | `"use client"`, floating button to open sidebar on mobile (`md:hidden`) |
| CommandPalette | `app/(app)/CommandPalette.tsx` | `"use client"`, Cmd+P search overlay, searches tags |
| TagInput | `app/(app)/notes/[id]/TagInput.tsx` | `"use client"`, tag editing with autocomplete |

### Layout

- `app/(app)/layout.tsx` — Server component: fetches notes + todos, wraps pages in `SidebarProvider` + `AppSidebar` + `SidebarInset`
- No mobile header — sidebar is opened via `MobileSidebarFab` (floating action button, `md:hidden`)
- Route group `(app)` contains both `notes/` and `todos/`

### Pages

```
app/(app)/notes/page.tsx       → Empty state (no note selected)
app/(app)/notes/[id]/page.tsx  → Edit + upload
app/(app)/todos/page.tsx       → Eisenhower Matrix (2x2 grid)
```

### Installed shadcn/ui Components

button, card, input, dialog, textarea, badge, sidebar, separator, sheet, tooltip, skeleton, checkbox, select, command

## Rules

### Language

- All user-facing text (labels, buttons, placeholders, messages, tooltips, empty states) must be in German

### General

- Max 200 lines per file — split into smaller modules if exceeded
- TypeScript strict mode
- No database — filesystem only
- Tailwind responsive design
- `NOTES_ROOT` env var for data directory
- `revalidatePath()` after mutations
- Tests live in separate `*.test.ts` files next to the module they test (e.g. `lib/foo.test.ts` for `lib/foo.ts`)
- File naming: PascalCase for components/pages, camelCase for lib/utils, lowercase for config
- Before writing any UI code, check `components/ui/` for an existing shadcn component first
- Never implement custom UI components from scratch. First check if a matching shadcn/ui component exists (`npx shadcn@latest add <name>`). Only build custom components when no shadcn equivalent is available.
- Run `bun run lint` and `bunx tsc --noEmit` after every code change. Fix all errors before moving on.

### Bun

- Use `bun install` instead of `npm i`
- Scripts: `bun --bun next dev`

### Style System (shadcn/ui + Tailwind v4)

- All colors defined as CSS variables in `app/globals.css` (`:root` for light, `.dark` for dark)
- Registered via `@theme inline` so Tailwind generates utility classes
- Dark/light mode via `next-themes` (class-based, `.dark` on `<html>`)
- ThemeProvider in `app/providers.tsx`, wraps app in `app/layout.tsx`
- `cn()` helper in `lib/utils.ts` for conditional class merging

**Color tokens:** `background`, `foreground`, `card`, `card-foreground`, `popover`, `popover-foreground`, `primary`, `primary-foreground`, `secondary`, `secondary-foreground`, `muted`, `muted-foreground`, `accent`, `accent-foreground`, `destructive`, `destructive-foreground`, `border`, `input`, `ring`, `--radius`, `quadrant-do`, `quadrant-do-foreground`, `quadrant-schedule`, `quadrant-schedule-foreground`, `quadrant-delegate`, `quadrant-delegate-foreground`, `quadrant-planned`, `quadrant-planned-foreground`

**Shared CSS classes (`app/custom-components.css`):**

Global component classes live here so visual adjustments apply everywhere at once. Use plain CSS values — avoid `@apply` for anything beyond well-known Tailwind v4 utilities, as Turbopack's CSS pipeline can fail on unresolved utilities.

| Class | Used on | Controls |
|---|---|---|
| `.card-base` | `Card`, `MobileBottomNav` | `rounded-xl shadow-sm` — shared panel look (no border) |
| `.sidebar-inner` | Desktop `sidebar-inner` div, mobile `SheetContent` wrapper | `flex h-full w-full flex-col p-2 gap-2` |
| `.sidebar-panel` | Desktop `sidebar-inner` div, mobile `SheetContent` | `border-0 gap-2` — controls sidebar border |
| `.note-section-padding` | Note section elements | `px-4 py-2` |

**Component rules:**
- Use shadcn/ui from `components/ui/` for all interactive elements (Button, Card, Input, Dialog, Textarea, Badge, Checkbox, Select)
- Import with `@/components/ui/...`
- Use `cn()` from `@/lib/utils` for conditional classes — always use the object syntax (`cn({ "bg-accent": isActive })`) instead of `&&` short-circuits (`cn(isActive && "bg-accent")`)
- Prefer CVA variants over inline Tailwind overrides when a component needs multiple visual styles
- Never use raw HTML elements when a shadcn equivalent exists
- Never hardcode colors — use semantic tokens (`bg-primary`, `text-muted-foreground`)
- Never use `dark:` prefix — CSS variables handle light/dark automatically
- To add a color: define in `:root` + `.dark` in `globals.css`, register in `@theme inline`
- To add a shadcn component: `npx shadcn@latest add <component-name>`
- When adjusting visual style shared across multiple components (borders, shadows, rounding), add/update a class in `custom-components.css` rather than editing each component individually

### Approved Packages

```
@uiw/react-md-editor uuid zod gray-matter cmdk
shadcn/ui (via CLI)
next-themes radix-ui lucide-react
class-variance-authority clsx tailwind-merge tw-animate-css
@uiw/react-codemirror @codemirror/lang-markdown @codemirror/search @codemirror/commands @codemirror/view @codemirror/state
@serwist/turbopack serwist
```

### Never

- No database (Prisma, SQLite, etc.)
- No experimental Next.js features
- No wrong folder structure (see Architecture section)
- No external CSS frameworks (shadcn/ui is fine)
- No hardcoded colors (`bg-blue-500`, `#fff`, etc.)
- No `dark:` prefix for themed colors
- No new UI libraries without updating this file
- No custom styling that bypasses the design token system

## Docker

- Multi-stage `Dockerfile` using Bun
- `docker-compose.yml` with volume `./dev-notes:/app/data`
- See `README.md` for Synology deployment instructions
